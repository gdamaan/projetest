

<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_article') }}">Shop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_article') }}">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_artikle') }}">Articles
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% endif %}
                {% if not is_granted('ROLE_USER') %}
                <a class="nav-link active" href="{{ path('app_register') }}">Inscription
                    <span class="visually-hidden">(current)</span>
                </a>
                {% endif %}
                {% if not is_granted('ROLE_USER') %}
                <a class="nav-link active" href="{{ path('app_login') }}">Connexion
                    <span class="visually-hidden">(current)</span>
                </a>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>